# Session Handoff — Smart Clinic Landing/Booking Migration

**Read `SMART_CLINIC_MIGRATION_PLAN.md` first — it is the source of truth for process, phases, and rules.** `SMART_CLINIC_UI_ARCHITECTURE_REPORT.md` is the component/design reference. This file is just the fast-resume summary.

## Goal & Strategy
Migrate the public **landing page + booking flow only** to a new Figma design (`uAHSgG9yvEIj2eB3IZQDxg`, page "landing"). Additive, in-place migration — not a rewrite. Dashboard/CRM, authentication, and backend infra are explicitly **out of scope**. New design tokens replace the pastel system for landing/booking only; dashboard keeps its existing tokens untouched. Booking is frontend-only against a mock service — no real backend work at all this migration.

## Repo State
- Branch: `smart-clinic-1july`. As of Task 6 (2026-07-28), the entire migration (Phases 1–6 plus post-migration Tasks 1–6) is approved and committed/pushed — see the Migration Plan's Task 6 entry and §10 for full detail. Everything described in this file up through Task 6 reflects that commit.
- Pre-existing (not from this work, not included in this migration's commit): `.claude/settings.local.json` modified.
- `node_modules` now installed (`npm ci` was run in Phase 0 — didn't touch `package.json`/`package-lock.json`).
- `npm run build` and `npx tsc --noEmit` both pass clean as of last check (2026-07-28, after Task 6 — final QA before commit/push), bundle size unchanged (121 kB First Load JS on `/`) throughout.
- The `.next` build-cache directory is regenerated on demand (`npm run build` / `npm run dev`) and was deleted/rebuilt clean during Phase 3.6, Phase 4.1, Phase 4.2, Phase 5.0, and Phase 5.1 after cache-corruption incidents (same root cause every time — see Phase 3.6/4.1/4.2/5.0/5.1 entries below) — not a source-controlled directory, no action needed on resume beyond a normal `npm run dev`.
- Full interactive dashboard walkthrough (login → Dashboard → Patients → Calendar day-switching → Requests → Notifications → Settings toggle → logout) was actually verified in-browser for the first time in this migration during Phase 6 (2026-07-28) — all passed, no regressions. Previously this had only been server-render-checked (see Phase 0).
- Full regression coverage now confirmed for all three booking personas end to end: New Patient (success + duplicate-name error), Existing Patient/Iran (`111111`, success), Existing Patient/Abroad (`222222` success and `555555` forced-payment-failure + retry) — see Migration Plan's Phase 6 entry for detail.

## Completed Phases
- **Phase 0 — Safety Checkpoint:** baseline recorded. `npm run build` succeeds. Dashboard walkthrough only *partially* verified — landing page confirmed server-rendering correctly, but full interactive click-through (login → dashboard pages → logout) was never actually verified, since no browser-automation tool (chromium-cli/Playwright) is available in this environment.
- **Phase 1 — Design System Foundation:** new additive color scales (`primary`, `secondary`, `neutral`, `red`, `success`, `warning`, 50–900 each) and typography scale (`tiny`/`small`/`caption`/`body`/`h6`–`h1`) added to `tailwind.config.ts` + `app/globals.css`. Google Fonts import extended for Vazirmatn Thin/ExtraBold weights.
- **Phase 2 — Shared UI Primitives (5 of 8 done, and staying that way):** `Button`, `Input`, `Card`, `Modal`, `StepperIndicator` built in `components/ui/`, grounded in real Figma pulls (not screenshots). **`StatusScreen`, `IconCard`, `SectionHeading` — Phase 2b canceled (2026-07-20).** These three are **not** standalone shared primitives in this migration. If their visual pattern is needed in Phase 3/4/5, implement it locally inside the relevant page/domain component — do not add them to `components/ui/` by default. A shared primitive is only created if a real cross-location reuse need actually appears later, as a separate explicit decision. See Migration Plan Phase 2 Execution Log entry for detail.
- **Phase 3.0 — Landing Page Foundation/Layout Shell (2026-07-20):** `components/pages/LandingPage.tsx` rewritten to a skeleton — RTL root container with six ordered, labeled placeholder sections (Navbar/Hero/About/Services/Contact/Footer), no real content, no `BookingWizard` import, no modals. Styling decision (asked, not assumed): **Tailwind-only**, going forward — `app/landing.css` is no longer imported by the new component (file itself untouched on disk). `Alt+L` → `onLogin` kept working (dashboard stays reachable); `Escape` modal-close handler dropped (nothing to close yet). `onLogin`/`onBookingSubmit` props unchanged. Build/typecheck/dev-server all clean. Full Visual QA report delivered in-conversation.
- **Phase 3.1 + 3.2 — Header/Navbar + Hero, implemented and approved (bundled by explicit request):** built via a **manual screenshot-based workflow instead of Figma MCP** — you supplied screenshots, manual measurements, and final image/video/icon assets directly; the Figma MCP rate-limit blocker that originally paused this sub-phase was worked around this way rather than waiting for it to clear. Built from that supplied source, not from live Figma tool calls, for both sub-phases.
  - **Navbar (final):** real header built — logo (`/logo.png`), nav links (خانه/خدمات/تماس با ما), phone number with `/Phone, Call.svg` icon, "پشتیبانی" support `Button` with `/message-question-checkmark.svg` icon. Replaces its Phase 3.0 placeholder.
  - **Hero (final):** real section built — `/Hero-background.mp4` video background (`/Hero-background.jpg` as `poster`), RTL headline/subhead copy, and the booking-entry card showing **only the first booking-form screen as static UI**: the New-vs-Existing persona toggle (local `useState`, no submission logic) and a "بعدی" button (`/Arrow.svg`) disabled until a persona is picked. No downstream steps, no `onBookingSubmit` wiring, no real booking logic — matches the Phase 3.2 scope note that the entry point stays stubbed until Phase 4+.
  - **About/Services/Contact/Footer:** still Phase 3.0 dashed placeholders, unchanged.
  - **Visual refinement round, completed and approved (2026-07-21/22) — two corrections to the initial build:**
    1. **Nav link color/hover behavior.** Root cause: `app/globals.css` has global `a { color: var(--accent) }` / `a:hover { text-decoration: underline }` rules declared *outside* any `@layer` block; under Tailwind v3's cascade-layers compilation, unlayered CSS wins over layered utility classes regardless of specificity, so the plain `hover:text-primary-500`/`hover:no-underline` utilities were silently losing. Fixed at the anchor level in `LandingPage.tsx` (not by touching the shared `globals.css`) using Tailwind's `!important` modifier: `!text-black`, `hover:!text-primary-500`, `hover:!no-underline`. Verified in the compiled CSS: default `color: rgb(0 0 0) !important`, hover `color: var(--primary-500) !important`, hover `text-decoration-line: none !important`.
    2. **Hero text shadow.** The heading/subtext originally used a `drop-shadow-[...]` filter utility, which wasn't producing the intended effect; replaced with a literal `[text-shadow:...]` arbitrary-property utility on the `<h1>`. Verified in compiled CSS on the heading: `text-shadow: 0 6px 17px rgba(0,0,0,0.49)`. **Current state, flagged:** the subtext (`<p>`) now carries a *different* shadow value — `[text-shadow:0_6px_7px_rgba(0,0,0,0.65)]` (blur 7px, opacity 65%) — rather than the same value as the heading; this diverged from the original single shared-shadow instruction via an edit made outside this conversation (file changed on disk after the corrections were applied and verified). Recorded here as the actual current state, not corrected, since this file update is documentation-only.
  - Other visual refinement passes on Navbar/Hero beyond the two corrections above were completed prior to this record (exact count/detail not separately logged).
  - Old `public/hero.jpg` removed as part of this work (superseded by `/Hero-background.jpg` + `/Hero-background.mp4`).
  - **Re-verified 2026-07-22 (after the two corrections and the later on-disk edit above):** `npx tsc --noEmit` clean; `npm run build` succeeds with only the same two pre-existing, unrelated warnings (`page.tsx` `useEffect` dependency, `layout.tsx` viewport metadata) — no new warnings. Dev server started and the compiled CSS actually served (`/_next/static/css/app/layout.css`) was inspected directly to confirm the `!important` nav rules and the heading's `text-shadow` rule compiled exactly as intended — not just that the class names appear in markup.
  - **Not yet done:** a written Visual QA report in the full §7 format for this sub-phase has not been produced/recorded, despite the refinement passes described above; this is flagged, not silently skipped. Full Alt-tab/browser-based visual QA against the supplied screenshots (side-by-side or overlay) has not been logged either — verification so far is build/typecheck/compiled-CSS-level, not a documented visual comparison.
  - **Status: implementation approved by you; proceeding to the next phase is pending your go-ahead.**
- **Phase 3.3 — About/Doctor bio section ("Section 2"), implemented and approved:** built from your supplied screenshot (`public/section2.png`) and exact manual measurements — same manual-workflow pattern as Phase 3.1/3.2, not Figma MCP. Replaces the Phase 3.0 `about` placeholder as its own full-width `<section>` directly following the Hero, with zero gap between them.
  - **Final implementation:** heading "دکتر محمد مهدی اسماعیل‌زاده پوریا" (Vazirmatn Medium, `text-h3`/40px, centered, 56px from section top, `neutral-900`); subtitle "متخصص روان‌شناس سلامت | عضو هیئت علمی دانشگاه" (Vazirmatn Light, `text-h5`/24px, centered, `neutral-700`); intro line "روان‌درمانگر از سال ۱۳۸۰، با تمرکز بر:" pinned 200px from the section top (Vazirmatn Light, `text-h5`/24px, centered, `neutral-900`); a 2×2 feature grid (رویکردهای اگزیستانسیالیسم / طرح‌واره درمانی اختلالات روانی و شخصیتی / زوج‌درمانگر و خانواده‌درمانگر / روان‌درمانگر سرطان (سایکوآنکولوژیست)) in Vazirmatn Medium/24px/`neutral-900`, each with a `/Check-one.svg` icon (24×24px, 8px gap) positioned to the **right** of its text per RTL, matching the screenshot exactly. All text in the section uses `leading-[150%]`. Section background is a top-to-bottom **linear** gradient between white and light blue (`#ffffff` → `#CCD7FF`).
  - **Correction made during this phase (final state only — intermediate attempt not carried forward):** the section was initially built with a radial gradient and icons placed to the left of their text (icon-then-... ordering mistake); per your explicit correction this was changed to a linear gradient (same two colors) and the icon/text DOM order was swapped so icons render to the **right** of their text, matching the screenshot. Only this corrected version is the recorded implementation.
  - **Verified 2026-07-22:** `npx tsc --noEmit` clean; `npm run build` succeeds with only the same two pre-existing, unrelated warnings — no new ones. Visually sanity-checked in-browser at 1440px.
  - **Not yet done:** a written §7-format Visual QA report has not been produced for this sub-phase (same open item as 3.1/3.2). Feature-grid column/row gap values (`gap-x-24`/`gap-y-6`) and the feature grid's vertical position (`top-[268px]`) were inferred from the screenshot's proportions rather than given as exact pixel numbers — flagged as a judgment call, not yet explicitly confirmed by you.
  - **Status: implementation approved by you; proceeding to the next phase is pending your go-ahead.**
- **Phase 3.4 — Services section ("Section 3"), implemented and approved:** built from your supplied screenshot (`public/section3.png`) and exact manual measurements — same manual-workflow pattern as 3.1–3.3, not Figma MCP. Replaces the Phase 3.0 `services` placeholder as its own full-width `<section>` directly following Section 2 (About), with zero gap between them; `683px` fixed height, white background.
  - **Final implementation:** heading "خدمات تخصصی کلینیک روان‌درمانی پوریا" (Vazirmatn Medium, 40px, `leading-[150%]`, centered, 55px from section top, `neutral-900`); subtitle "با بهره‌گیری از آخرین دستاوردهای علمی در حوزه روان‌شناسی، خدمات جامع و متنوعی را به شما ارائه می‌دهیم" (Vazirmatn Light, 24px, `leading-[150%]`, centered, `neutral-700`, 8px below the heading). Two decorative flower images (`/big-flower.jpg` at 215×732px/x:1231,y:-24 and `/little-flower.jpg` at 246×339px/x:-13,y:355, both non-interactive) positioned per the spec's raw canvas coordinates (left/top, not RTL-mirrored — these are absolute design-canvas positions, not text-flow-relative). A 2×2 grid of four `540×188px` cards (`8px` radius, `1px solid neutral-100` border, `0 6px 20px rgba(0,0,0,0.04)` shadow), 24px gaps both axes, first row 223px from the section top, centered via the same 168px side-margin convention already established in the Hero section. Each card: a 40×40px colored icon (`/Brain.svg`, `/People.svg`, `/Family.svg`, `/Person-heart.svg` — each already has its own baked-in stroke color and `neutral-50` rounded background, matching the screenshot) at 24px from the top/right; a title (Vazirmatn Medium, 24px, `leading-[150%]`, `neutral-900`) at 24px right / 72px top; a description (Vazirmatn Light, 16px, `leading-[150%]`, `neutral-700`, 492px wide) at 24px right / 116px top. Card order/content, all copied verbatim from the screenshot: روان‌درمانی اختلالات روانی (Brain, top-right) / درمان اختلالات شخصیتی (People, top-left) / زوج‌درمانی و خانواده‌درمانی (Family, bottom-right) / مشاوره قبل از ازدواج (Person-heart, bottom-left).
  - **Flagged, not a correction:** the spec named the flower assets `big-flower.png`/`little-flower.png`; the files actually present on disk are `big-flower.jpg`/`little-flower.jpg` (no `.png` versions exist). Used the actual `.jpg` files present — flagged for your awareness, not treated as a blocker, consistent with how the Phase 3.1 logo/hero-asset filename mismatches were previously handled.
  - **Flagged, inferred rather than given:** the card shadow's exact blur radius (spec said "match the screenshot" without a number) was estimated at 20px; the section background color (white) was read directly from the screenshot since the spec didn't state it explicitly. Both are visual judgment calls, not numeric spec values — not yet explicitly confirmed by you.
  - Approved on first pass in-conversation — no correction round was needed for this sub-phase (unlike 3.3's radial→linear/icon-side correction).
  - **Verified 2026-07-22:** `npx tsc --noEmit` clean; `npm run build` succeeds with only the same two pre-existing, unrelated warnings — no new ones. Visually sanity-checked in-browser at 1440px.
  - **Not yet done:** a written §7-format Visual QA report has not been produced for this sub-phase (same open item as 3.1–3.3).
  - **Status: implementation approved by you; proceeding to the next phase is pending your go-ahead.**
- **Phase 3.5 — Contact section ("Section 4"), implemented and approved:** built from your supplied screenshot (`public/section4.png`) and exact manual measurements — same manual-workflow pattern as 3.1–3.4, not Figma MCP. Replaces the Phase 3.0 `contact` placeholder as its own full-width `<section>` directly following Section 3 (Services), with zero gap between them; `642px` fixed height.
  - **Final implementation:** background image (`/section-background.png`, cover/center) with a black overlay on top; heading "در تماس باشید" and subtitle "پاسخگوی سوالات شما هستیم" (Vazirmatn Medium 40px / Light 24px respectively, both `leading-[150%]`, centered, white, subtitle 8px below the heading). A 2×2 grid of four glassmorphism cards (currently `352×152px` on disk — the spec's `144px` was adjusted slightly outside this correction round; recorded as the actual current state, not reverted; `8px` radius, thin `#D3D7DC` border, white-13%-opacity fill, `backdrop-blur`), 24px gaps both axes, first row 210px from the section top. Each card: a `32×32px` icon wrapper (same border/fill treatment as the card) holding a centered `24×24px` icon, 16px from the card top; a title (Vazirmatn Light, 16px, `leading-[150%]`, white, centered) below the icon; a value area (Vazirmatn Medium, 16px, `leading-[150%]`, white) below the title. Card order/content, right-to-left/top-to-bottom: آدرس کلینیک (`/Pin.svg`) / شماره تماس (`/Phone 2.svg`) / پست الکترونیک (`/Emails.svg`) / ساعات پذیرش (`/Clock.svg`), all copied verbatim from the screenshot.
  - **"ساعات پذیرش" (hours) card, final layout:** unlike the other three cards' single centered value line, this card renders two label/value rows (تمام ایام به غیر از تعطیلات رسمی ↔ ۱۵:۰۰–۲۱:۰۰, and پنجشنبه ↔ ۱۰:۰۰–۱۳:۰۰), each row's day-label anchored to the card's right edge and hour-range anchored to the left edge, 16px inset from both edges, matching the screenshot's two-column arrangement rather than a single centered paragraph.
  - **Correction round, completed and approved — three fixes to the initial build (only the corrected/final version is recorded as the implementation):**
    1. **Card order.** The first pass ordered cards شماره تماس / آدرس کلینیک / ساعات پذیرش / پست الکترونیک (right-to-left, top-to-bottom) — a misreading of the reference screenshot. Per your explicit correction, reordered to آدرس کلینیک / شماره تماس / پست الکترونیک / ساعات پذیرش.
    2. **Hours-card layout.** The first pass rendered the hours card as a single centered two-line paragraph (label and time on the same line, second line for Thursday). Per your explicit correction, rebuilt as two label/value rows with the day-label right-aligned (16px from the right edge) and the hour-range left-aligned (16px from the left edge), matching the screenshot's actual two-column layout.
    3. **Border thinness.** Card and icon-wrapper borders were visually too heavy at full-opacity `#D3D7DC`; changed to `#D3D7DC` at 60% opacity (width unchanged at `0.5px`) to better match the screenshot's subtler line weight.
  - **Verified 2026-07-22:** `npx tsc --noEmit` clean; `npm run build` succeeds with only the same two pre-existing, unrelated warnings — no new ones. Visually sanity-checked in-browser at 1440px after the correction round.
  - **Not yet done:** a written §7-format Visual QA report has not been produced for this sub-phase (same open item as 3.1–3.4).
  - **Status: implementation approved by you; proceeding to the next phase is pending your go-ahead.**
- **Phase 3.6 — Footer, implemented and approved. This completes Phase 3 (the entire static landing page) end to end.** Built from your supplied screenshot (`public/Footer.png`) and exact manual measurements — same manual-workflow pattern as 3.1–3.5, not Figma MCP. Replaces the last Phase 3.0 placeholder as its own full-width `<section>` directly following Section 4 (Contact), `15px` gap above it (the only section-to-section gap in the whole page that isn't `0px`); `320px` fixed height, white background. The now fully-unused `SectionPlaceholder` helper component was removed as dead code in the same edit (no remaining call sites, not a design change).
  - **Final implementation:** logo (`/logo.png`, 128×51px) at 80px from top / 80px from right; nav links خانه/خدمات/تماس با ما (Vazirmatn Regular, 16px, `leading-[150%]`, black, centered as a group, 94px from top — same `gap-8`/32px spacing as the header's identical link set); social icons row at 94px from top with `Instagram.svg`/`LinkedIn.svg` (24×24px, 12px gap); a 1280px-wide, 1px solid black divider, centered, 187px from top; copyright line "تمام حقوق وبسایت کلینیک روان‌درمانی پوریا محفوظ است." (Vazirmatn Regular, 14px, `leading-[150%]`, black, centered, 219px from top).
  - **Judgment call, not independently re-verified:** the spec gave both "the rightmost link is 409px from the logo" and "the links are centered within the footer" for the nav — these two are largely consequential of each other rather than independent constraints, so only the stated "centered" positioning was implemented directly; the 409px figure was treated as a derived reference value, not separately enforced. Flagged, not yet explicitly confirmed by you.
  - **Correction round, completed and approved — three fixes to the initial build (only the corrected/final version is recorded as the implementation):**
    1. **Social icon order.** In an RTL flex row, DOM order determines which icon lands on which side; the first pass put Instagram first in the DOM, which rendered it on the right (not matching the screenshot's Instagram-on-the-left layout). Corrected by swapping the DOM order (LinkedIn first, Instagram second), which now renders Instagram at the required 80px-from-left position with LinkedIn to its right.
    2. **Social icons made clickable.** Both icons wrapped in plain `<a href="#">` (no added classes, so sizing/spacing unchanged) as placeholder links, per your explicit request — real URLs to be added later.
    3. **Nav link hover behavior.** Same root cause and fix already applied to the header nav in Phase 3.1: `app/globals.css`'s unlayered `a { color: var(--accent) }` / `a:hover { text-decoration: underline }` rules beat plain Tailwind utilities regardless of specificity. Added `hover:!text-primary-500 hover:!no-underline` (default `!text-black` was already in place) — verified in the actual compiled CSS served by the dev server: `.hover\:\!text-primary-500:hover{ color: var(--primary-500) !important; }` (a single shared rule, reused by both the header's and footer's six links combined, since Tailwind dedupes identical utility classes).
  - **Not a code defect, flagged for the record:** mid-phase you reported "the page lost all styling" after a manual edit. Investigated and found the actual cause was **`.next` build-cache corruption**, not a code or JSX error — I had been running `npm run build` for verification while the long-running `npm run dev` server was simultaneously active, and both processes write to the same `.next` directory, corrupting the dev server's webpack module cache (`Cannot find module './611.js'`, `__webpack_modules__[moduleId] is not a function`). `LandingPage.tsx` built cleanly on its own throughout. Fixed by stopping the dev server, deleting `.next`, and restarting clean — no source file was reverted or changed as part of that fix. **Process change adopted for the rest of this phase:** the dev server is now stopped before each `npm run build` verification run and restarted (with `.next` cleared) afterward, to prevent recurrence.
  - **Verified 2026-07-22:** `npx tsc --noEmit` clean; `npm run build` succeeds with only the same two pre-existing, unrelated warnings — no new ones, across all three correction rounds. Dev server restarted clean after each round and the compiled CSS was inspected directly (not just class names in markup) to confirm the hover rule.
  - **Not yet done:** a written §7-format Visual QA report has not been produced for this sub-phase (same open item as 3.1–3.5) — and, now that Phase 3 is complete end to end, a consolidated Phase 3 Visual QA pass has also not been produced.
  - **Status: implementation approved by you; proceeding to Phase 4 is pending your go-ahead.**
- **Phase 4.0 — Booking controller skeleton, implemented (2026-07-22).** `components/booking/useBookingFlow.ts` created — a `useReducer`-based hook holding `{ persona: 'new' | 'existing' | null }` with a single `SET_PERSONA` action, exposed as `{ state, setPersona }`. `lib/mock/bookingService.ts` created as a scaffold — `BookingServiceApi` interface (`submitBooking`/`verifyOtp`/`lookupPatientByPhone`), each method a stub throwing "not implemented yet" (you explicitly chose this over a bare in-memory-store-only scaffold, since §5's method shapes aren't decided until later phases — see Migration Plan Phase 4.0 entry for the full ambiguity/decision record). Hero's persona buttons in `LandingPage.tsx` rewired from a local `useState<Persona>` to the new hook; `BookingPersona` type moved out of the page into the hook module. No markup/class/copy changes — only the state source changed.
  - **Verified 2026-07-22:** `npx tsc --noEmit` clean; `npm run build` succeeds with only the same two pre-existing, unrelated warnings. Browser-verified (dev server + real DOM click events, no screenshots per your instruction): initial state has neither persona selected and "بعدی" disabled; clicking either persona button selects it exclusively (`border-primary-500 bg-primary-50 text-primary-500` classes applied) and enables "بعدی"; captured class strings matched the pre-existing markup exactly before and after, confirming no visual change and that `useBookingFlow` state is what drives the UI.
  - **Not yet done:** intake form, review step, terminal states, and real `bookingService` method implementations — all deferred to Phase 4.1/4.2.
  - **Status: Approved (2026-07-26) — proceeded to Phase 4.1.**
- **Phase 4.1 — New Patient Intake Step, implemented and approved (2026-07-26).** `components/booking/NewPatientIntakeStep.tsx` created (heading, `StepperIndicator` at پرونده/done–اطلاعات/current–پیگیری/pending, four fields — نام و نام خانوادگی/شماره تماس/نام معرف required, دلیل مراجعه optional textarea — and a قبلی/بعدی footer), built from `public/information.png`, `public/info-entered-and-error.png`, `public/Inputs.png`, `public/Default Reserve.png`. `useBookingFlow.ts` extended with a `step` field (`'entry' | 'new-intake'`), the intake form's data/errors/submitting state, and `goToNewIntake`/`goToEntry`/`setIntakeField`/`submitNewPatientIntake`. `bookingService.ts` gained `checkNewPatientName` — a mock async duplicate-name check (400ms latency) against a static fixture (`['حسین ملکی']`), returning the exact Figma error copy "این نام قبلا ثبت شده است" on a match. `Input.tsx`'s multiline height was corrected `180px`→`150px` and `StepperIndicator.tsx`'s label font corrected `tiny`(10px)→`small`(12px) — both were the first real consumers of those primitives and didn't match this phase's exact spec.
  - **Scope conflict surfaced and resolved (per §0):** your written requirements excluded Review/terminal-flow work this phase, but the supplied references included terminal-state screenshots/specs (request-error/success/no-service). You confirmed: intake form only; terminal states stay Phase 4.2. "بعدی" on the intake form validates and then dead-ends by design (no review step exists yet) — same pattern as the Hero stub between Phase 3.2 and 4.0.
  - **UI architecture corrected mid-phase (your explicit request):** booking wizard presentation changed from a centered `Modal` overlay (the initial build) to an **in-place resize of the Hero's own booking panel** — no backdrop/dimming, no separate dialog; the panel itself grows/shrinks between the persona-choice and intake views. `Modal` itself is untouched, just no longer used here.
  - **Sizing iterated with your review, final state:** entry/default panel is `524×212px` (pixel-matched to `public/Default Reserve.png`; was an uncalibrated `524×236px` guess), "بعدی" offset `mt-[28px]`. New Patient intake panel is currently `524×712px` at `top-[34px]`, inside a Hero section you widened to `780px` — both the Hero height and these exact panel values were adjusted directly in the file outside this conversation after my last pass (`650px`/`top-[8px]`); recorded here as the actual, approved current state.
  - **Verified 2026-07-26:** `npx tsc --noEmit` clean; `npm run build` succeeds with only the same two pre-existing warnings. Interaction verified end-to-end via browser automation (persona select → panel resize/reposition → validation → duplicate-name error).
  - **`.next` cache-corruption recurrence (resolved, not a code defect):** same root cause as the Phase 3.6 incident (module-not-found error from `npm run build` and `npm run dev` writing to `.next` concurrently), different module number (`./833.js` this time). Fixed the same way: stop dev server, delete `.next`, restart clean.
  - **Not yet done:** review step, terminal states (Success/Error/No-service), `submitBooking`'s real implementation — all Phase 4.2, deliberately excluded per your confirmation above.
  - **Status: Approved (2026-07-26) — proceeded to Phase 4.2.**
- **Phase 4.2 — New Patient Review + Terminal States, implemented and approved (2026-07-26). This completes the entire New Patient persona end to end.** `components/booking/NewPatientTerminalStep.tsx` created — the three terminal screens (Success/Error/No-service), built from `public/request-success.png`/`request-error.png`/`no-service.png`, icons `done.svg`/`xmark.svg`/`warning.svg` in a 48×48px/24px-radius wrapper (`success-50`/`red-100`/`warning-100`). `bookingService.ts`'s `submitBooking` implemented for real (was a stub). `useBookingFlow.ts` gained a `new-terminal` step, submission logic, `retryIntake`, `resetBookingFlow`. The Hero booking panel (still no modal) now also resizes to `524×220px` for terminal states, vertically centered at `top-[280px]` within the `780px` Hero section — same pattern as the intake step.
  - **Scope conflict surfaced and resolved (per §0):** the architecture discovery notes describe a distinct "Info completed (review)" screen between the intake form and terminal states, but no supplied screenshot shows it. You confirmed **no review screen exists in the design** — submission goes straight from the intake form to a terminal state.
  - **Business-logic decision surfaced and resolved (per §0):** with no real backend, there's no rule to pick a submission's outcome. You approved a static mock `onlineBookingEnabled` flag (forces No-service when off) plus two demo name-substring triggers, in this priority: full name contains `ارور` → Error; else flag off or name contains `تعطیل` → No-service; else → Success.
  - **Terminal actions:** Success's "بازگشت" fully resets the flow to the persona-choice entry screen. Error's "تلاش مجدد" returns to the intake step with entered data preserved (not cleared). No-service has no button at all, matching its screenshot exactly — a genuine dead end by design.
  - **Bug found and fixed after your first review:** the initial implementation checked the No-service condition before the Error trigger, and used the wrong trigger word (`خطا` instead of `ارور`) — so Error was unreachable. Fixed in `bookingService.ts` only: reordered the check (Error first) and corrected the trigger word. Re-verified all three states plus the both-triggers-present priority case.
  - **Verified 2026-07-26:** `npx tsc --noEmit` clean; `npm run build` succeeds with only the same two pre-existing warnings. All four outcomes (Success/Error/No-service/both-triggers-present) and the Phase 4.1 duplicate-name regression verified end-to-end via browser automation.
  - **`.next` cache corruption recurred a third time** (same root cause as Phase 3.6/4.1); resolved the same way. Going forward: stop the dev server before running `npm run build`, rather than let both run concurrently.
  - **Status: implementation approved (2026-07-26); proceeding to Phase 5.0 is pending your go-ahead.**
- **Phase 5.0 — Existing Patient shell + Iran flow, implemented and approved (2026-07-27), after one correction round. This completes the Existing Patient / Iran persona end to end.** Iran only, per the confirmed sequencing decision — Abroad is deferred to Phase 5.1. `lib/jalali.ts` created (Jalali calendar math, adapted from `BookingWizard.tsx`, which stays untouched). Five new step components created: `ExistingPatientCodeStep`, `ExistingPatientDateStep`, `ExistingPatientTimeStep`, `ExistingPatientCheckoutStep`, `ExistingPatientTerminalStep` — the full Reference Code → Date → Time → Checkout → Terminal flow. `bookingService.ts`'s `verifyReferenceCode`/`submitAppointmentPayment` implemented for real. `useBookingFlow.ts` extended with the full existing-patient state machine. The Hero booking panel (still no modal) resizes/repositions per step, same pattern as New Patient.
  - **Business-logic decision (per §0):** region (`'iran'`/`'abroad'`) is resolved purely from the reference code — no separate region-detection step. `111111` → Iran patient; `222222` → Abroad patient (shows a "not implemented in this version" message, Phase 5.1 scope); `444444` → a dev-only demo code that resolves as the same Iran identity as `111111` but is hardcoded to always fail at the payment step, so the Error terminal state is reachable on demand; anything else → invalid-code error.
  - **Self-tested before your review:** invalid code, `111111` full flow to payment success, `444444` payment failure → retry preserving selections, `222222` graceful not-implemented message, New Patient regression unaffected.
  - **Correction round, completed and approved — seven refinements requested after your review:**
    1. Calendar popup height `362px`→`372px` and date-selection panel height `630px`→`640px` (both `+10px`).
    2. Month navigation direction corrected: right arrow (above Saturday) now goes to the previous month, left arrow (above Friday) now goes to the next month — was reversed.
    3. Month/year labels (previously decorative carets only) now open clickable dropdown lists to jump directly to a month/year. Year list bounded to today's year `+5` — a judgment call, not yet explicitly confirmed by you.
    4. `isDayAvailable` (`lib/jalali.ts`) changed from "day ≥ today" to "day > today" — today itself is no longer bookable, only strictly future days. The calendar already used the real system clock via `getJalaliToday()`.
    5. Calendar header restructured to the same `grid-cols-7` layout as the weekday/day-cell grids below it, so the right arrow aligns exactly with Saturday's column and the left arrow with Friday's — removed a small header-only padding that had offset them.
    6. "قبلی"/"بعدی" in the Code and Date steps repositioned to exactly 24px from the panel's bottom edge (flex-column + `mt-auto`, using the panel's existing 24px padding), instead of a fixed top margin.
    7. Payment Success screen: info table moved a further 10px down (`mt-6`→`mt-[34px]`); "بازگشت" repositioned to the same 24px-from-bottom treatment as point 6.
  - **`.next` cache corruption recurred a fourth time** (same root cause as Phase 3.6/4.1/4.2 — `npm run build` run while `npm run dev` was concurrently active). Fixed the same way: stopped the dev server, deleted `.next`, restarted clean; verified compiled CSS/JS served 200 before resuming. No source file was affected.
  - **Verified 2026-07-27:** `npx tsc --noEmit` clean; `npm run build` succeeds with only the same two pre-existing, unrelated warnings — no new ones, after the initial implementation and the correction round.
  - **Not yet done:** Existing Patient / Abroad (Phase 5.1); Phase 6 integration/CTA wiring; a written §7-format Visual QA report (same standing open item as every phase since 3.1).
  - **Status: implementation approved (2026-07-27) — proceeding to Phase 5.1 is pending your go-ahead.**
- **Phase 5.1 — Existing Patient / Abroad extension, implemented and approved (2026-07-27), after four correction rounds. This completes the entire Existing Patient persona — Iran and Abroad both — end to end.** Extends the same shell Phase 5.0 built (region as a parameter, per §5), not a fork. New: `lib/mock/locations.ts` (`LocationRecord`: `id`/`city`/`country`/`countryFa`/`timezone`/`utcOffsetMinutes`, 15 sample cities) and `lib/locationService.ts` (the only module the UI talks to — `searchLocations`, `getLocationById`, `formatUtcOffset`, `convertIranHourToLocal`, `formatLocalTimeFa` — so the mock can later be swapped for a real backend without touching any component). `components/booking/existingPatientSteps.ts` created as a shared, region-aware stepper builder (Iran's 5 steps / Abroad's 6, with "مکان" inserted after "کد مرجع"), replacing per-file duplicated step lists. New `ExistingPatientLocationStep.tsx` (Abroad-only location/timezone step — searchable combobox, ~3 rows visible by default, rest via scroll). `lib/jalali.ts` extended with `toGregorian`/`gregorianShortLabel`/`fullDateLabelWithGregorian` for Abroad's dual Jalali+Gregorian calendar. `useBookingFlow.ts` extended with the `existing-location` step and `selectedLocation` state; `222222` (Abroad) now routes to the location step instead of the old "not implemented" error. `bookingService.ts` gained `555555` — an Abroad-only forced-payment-failure demo code, independent of Iran's `444444`.
  - **Self-tested before your review:** `222222`/`555555` → location step → search/select city → dual-calendar date step → dual-time-card time step → checkout → success/forced-failure; Iran (`111111`/`444444`) re-verified unaffected at every round; New Patient untouched.
  - **Correction rounds, completed and approved (only the final state recorded):**
    1. Abroad-only forced-failure code (`555555`); timezone-input border/radius/dropdown size (`476×218px`); label font sizes (12/16px); Date-selection panel+calendar `+50px`; Time-selection card styling, Abroad selected state, city-above-time layout.
    2. Discovered the height increase should have been Abroad-only — **Iran's Date/Checkout dimensions reverted** to their Phase 5.0-approved values (`640px`/`420px`/`372px` calendar). Root-caused why the card `#FCFCFC`/`0.5px neutral-100` styling wasn't rendering: `app/globals.css`'s unlayered `button { border: none; background: none; color: inherit; }` reset (same mechanism as the Phase 3.1 nav-link bug) silently wins over plain Tailwind classes on any `<button>` — fixed with `!important` (same pattern `Button.tsx`'s secondary variant already used). Country name (not city) shown in time cards; separator line corrected to `295px`/`neutral-100` beside the heading; Abroad calendar `402px`→`392px`.
    3. Country name switched English→Farsi (added `countryFa` to `LocationRecord`); Abroad calendar `392px`→`382px`; the location-trigger's own border needed the same `!important` fix; foreign time collapsed to one line ("{countryFa} - {time}") at the smallest type-scale token (`text-tiny`, 10px) with an ellipsis fallback — a static reduced size, not true per-string dynamic shrinking (no JS text-measurement elsewhere in this codebase); four labels set to 12px in both regions.
    4. "ساعت مراجعه را انتخاب کنید" set to `neutral-800` with an 8px gap before "ساعت‌های آزاد"; the same 295px divider added to **Iran's** Time step too (previously Abroad-only — this row is no longer region-branched in code); New Patient's "اطلاعات تماس خود را وارد کنید" set to 12px; Abroad Date panel `670px`→`660px`.
  - **`.next` cache corruption recurred once more during this phase** (same root cause as Phase 3.6/4.1/4.2/5.0); resolved the same way each time — stop dev server, delete `.next`, restart clean.
  - **Verified 2026-07-27:** `npx tsc --noEmit` clean; `npm run build` succeeds with only the same two pre-existing, unrelated warnings, after the initial implementation and all four correction rounds.
  - **Not yet done:** Phase 6 integration/CTA wiring; a written §7-format Visual QA report (same standing open item as every phase since 3.1).
  - **Status: implementation approved (2026-07-27) — proceeding to Phase 6 is pending your go-ahead.**
- **Phase 6 — Integration & Polish, implemented and approved (2026-07-28). This completes the entire landing + booking migration end to end.** `LandingPage.tsx`'s four Services-section cards now scroll smoothly to the Hero booking panel on click (keyboard-accessible: `role="button"`, `tabIndex`, Enter/Space, `cursor-pointer`). Contact section's phone/email cards became real `tel:`/`mailto:` links matching the header's existing pattern; address/hours cards stay informational-only; footer social icons remain placeholders (per the Phase 3.6 decision). `lib/utils.ts` had `getJalaliDate`/`formatJalaliDate`/`getDateFromDayOfWeek`/`computeSlots`/`monthNames` removed as confirmed-unused (the first two were also producing wrong output — year 1915 instead of ~1405); `dayNames`/`getWeekDayLabel` kept, since dashboard `CalendarPage.tsx` depends on them. `BookingWizard.tsx` untouched, per §3/§9's rule that its retirement is a separate later discussion.
  - **Regression testing spanned two sessions** (first interrupted by a usage limit mid-Abroad-flow): session 1 verified the dashboard walkthrough end-to-end for the first time in this migration (previously blocked by no browser tool), the Iran flow, service-card scroll, contact links, and the full New Patient persona (success + duplicate-name error). Session 2 resumed after resolving a two-Chrome-instances browser-automation connection mix-up, then completed the Existing Patient / Abroad flow: `222222` (Tokyo) → full success with reference number; `555555` (Abroad forced-failure demo code, Berlin) → payment-failed screen → retry preserves selections. A quick Iran re-check (`111111`) confirmed no regression from the Abroad-flow testing. No console errors; only the two pre-existing build warnings. See the Migration Plan's Phase 6 entry for full detail.
  - **Status: implementation approved (2026-07-28) — the entire landing + booking migration (Phases 1–6) is now complete and regression-tested end to end.**

## Decisions That Must Not Be Reversed
- Dashboard is fully frozen — no dashboard file touched, ever, without a separate explicit request.
- Mock auth (`lib/auth.tsx`, `LoginPage.tsx`) stays exactly as-is — no Cognito, no real login, this whole migration.
- No backend work of any kind (Lambda/API Gateway/DynamoDB/Cognito) — mock service only, in-memory first → static fixtures → `localStorage` only if a real need for reload-persistence emerges *and* is explicitly approved.
- Neutral color scale is named **`neutral`**, not `gray` — deliberate, to avoid overriding Tailwind's default `gray-*`, which `PatientsPage.tsx` (dashboard) relies on. Do not rename back.
- Pastel tokens (`sky`/`sage`/`beige`/`lavender`/`rose`) stay untouched — `Patient.color` (dashboard) depends on them.
- New-vs-Existing-patient and Iran-vs-Abroad are **real business-logic branches**, not visual variants — keep them extensible (region as a parameter on one shared flow, not forked components).
- ~~Old `components/BookingWizard.tsx` stays in place, untouched, until the entire new booking flow is built and verified end-to-end.~~ **Superseded (2026-07-28, Task 4):** you were explicitly asked whether to override this rule as part of the post-migration cleanup, and chose to remove `BookingWizard.tsx` and `app/landing.css` now. Both are deleted — see Files Touched below.
- New booking-step presentation is an **in-place resize of the Hero's own booking panel**, not a `Modal` overlay — decided explicitly in Phase 4.1 after you reviewed and rejected the initial Modal-based build, and carried through Phase 4.2's terminal states too. `Modal` stays available as a primitive but isn't used by this flow.
- The New Patient flow has **no review/confirmation screen** — confirmed explicitly in Phase 4.2; submission goes directly from the intake form to a terminal state (Success/Error/No-service).
- **Every phase/sub-phase requires explicit user approval before starting — no auto-continuation, ever**, even after a clean verification.
- **No silent design/UX/architecture/business-logic decisions** — stop and report ambiguity (with options + a recommendation) before implementing anything unclear.
- No git write operations (branch/commit/tag) without explicit per-instance approval.
- Figma scope is **desktop only** (1440px) — no responsive/mobile work unless explicitly requested later.

## Files Touched So Far
**Modified:** `clinic-crm/app/globals.css`, `clinic-crm/tailwind.config.ts` (both additive only); `clinic-crm/components/pages/LandingPage.tsx` (Phase 3.0 skeleton rewrite, then Phase 3.1/3.2 Navbar+Hero, then Phase 3.3 About/Doctor-bio, then Phase 3.4 Services, then Phase 3.5 Contact, then Phase 3.6 Footer built into it — the `SectionPlaceholder` helper was removed in the Phase 3.6 edit, having no remaining call sites; then Phase 4.0 rewired the Hero persona buttons from local `useState` to `useBookingFlow`; then Phase 4.1 made the Hero booking panel resize in place to show the New Patient intake step, and widened the Hero section to `780px` tall; then Phase 4.2 extended the same panel to also resize/reposition for the three terminal states; then Phase 5.0 wired in the five Existing Patient/Iran steps with their own panel sizes per step; then Phase 5.1 wired in the Location step and made panel heights region-aware; then Phase 6 wired the Services-section cards to scroll to the Hero booking panel and made the Contact section's phone/email cards real `tel:`/`mailto:` links); `clinic-crm/components/ui/Input.tsx` (Phase 4.1 — multiline height `180px`→`150px`); `clinic-crm/components/ui/StepperIndicator.tsx` (Phase 4.1 — label font `tiny`→`small`); `clinic-crm/lib/mock/bookingService.ts` (Phase 4.0 scaffold, Phase 4.1 `checkNewPatientName`, Phase 4.2 `submitBooking` implemented for real then a post-review bug fix to the trigger word/priority order, Phase 5.0 `verifyReferenceCode`/`submitAppointmentPayment` implemented for real, Phase 5.1 added the `555555` Abroad-only forced-failure demo code); `clinic-crm/components/booking/useBookingFlow.ts` (Phase 4.0 persona reducer, extended Phase 4.1 with intake-step state, extended Phase 4.2 with the terminal step + submission logic, extended Phase 5.0 with the full existing-patient state machine, extended Phase 5.1 with the `existing-location` step and `selectedLocation` state); `clinic-crm/lib/jalali.ts` (Phase 5.0 — created; Phase 5.1 — added `toGregorian`/`gregorianShortLabel`/`fullDateLabelWithGregorian` for Abroad's dual calendar); `clinic-crm/components/booking/{ExistingPatientDateStep,ExistingPatientTimeStep,ExistingPatientCheckoutStep}.tsx` (Phase 5.1 — extended with a `region` prop each, not duplicated); `clinic-crm/components/booking/NewPatientIntakeStep.tsx` (Phase 5.1 correction round 4 — intake heading font-size 12px, a shared-styling fix, not a New Patient scope change); `clinic-crm/lib/utils.ts` (Phase 6 — removed `getJalaliDate`/`formatJalaliDate`/`getDateFromDayOfWeek`/`computeSlots`/`monthNames` as confirmed-unused dead code, two of which also produced incorrect output; `dayNames`/`getWeekDayLabel` kept since dashboard `CalendarPage.tsx` depends on them); `clinic-crm/lib/jalali.ts` (Task 5 — added `parseDateKey()`, the inverse of `dateKeyFor()`, to deduplicate inline date-key parsing); `clinic-crm/lib/mock/bookingService.ts` and `clinic-crm/lib/locationService.ts` (Task 5 — removed each file's unused `export default`); `clinic-crm/components/booking/ExistingPatientTimeStep.tsx` (Task 5 — uses the new `parseDateKey()` instead of inline parsing); `clinic-crm/components/pages/LandingPage.tsx` (Task 5 — removed the unused `onBookingSubmit` prop, uses `parseDateKey()` instead of three inline duplicates); `clinic-crm/lib/locationService.ts` (Task 6 — added `getCountryNameFa()` to fix a `RangeError` crash on Kosovo/Somaliland entries in the real location dataset, see Task 6 entry above).
**Created:** `clinic-crm/components/ui/{Button,Input,Card,Modal,StepperIndicator}.tsx`; `clinic-crm/components/booking/NewPatientIntakeStep.tsx` (Phase 4.1 — intake form UI); `clinic-crm/components/booking/NewPatientTerminalStep.tsx` (Phase 4.2 — Success/Error/No-service screens); `clinic-crm/lib/jalali.ts` (Phase 5.0 — Jalali calendar math, adapted from `BookingWizard.tsx`); `clinic-crm/components/booking/ExistingPatientCodeStep.tsx`, `ExistingPatientTerminalStep.tsx` (Phase 5.0 — shared by both regions, no Phase 5.1 changes); `clinic-crm/lib/mock/locations.ts` + `clinic-crm/lib/locationService.ts` (Phase 5.1 — the mock location dataset and the dedicated service layer the UI talks to); `clinic-crm/components/booking/existingPatientSteps.ts` (Phase 5.1 — shared, region-aware stepper builder replacing per-file duplicated step lists); `clinic-crm/components/booking/ExistingPatientLocationStep.tsx` (Phase 5.1 — Abroad-only location/timezone step); `clinic-crm/SMART_CLINIC_MIGRATION_PLAN.md`; `clinic-crm/SMART_CLINIC_UI_ARCHITECTURE_REPORT.md`; this file; Phase 3.1–3.6 asset files under `clinic-crm/public/` — in active use: `logo.png`, `Hero-background.mp4`, `Hero-background.jpg`, `Phone, Call.svg`, `message-question-checkmark.svg`, `Arrow.svg`, `Check-one.svg`, `Brain.svg`, `People.svg`, `Family.svg`, `Person-heart.svg`, `big-flower.jpg`, `little-flower.jpg`, `Clock.svg`, `Emails.svg`, `Phone 2.svg`, `Pin.svg`, `section-background.png`, `Instagram.svg`, `LinkedIn.svg`; Phase 4.1 additions in active use: `Default Reserve.png`, `information.png`, `info-entered-and-error.png`, `Inputs.png` (all reference-only, not rendered in-page); Phase 4.2 additions now in active use: `request-success.png`/`request-error.png`/`no-service.png` (reference-only) and `done.svg`/`xmark.svg`/`warning.svg` (rendered in-page); Phase 5.0 additions now in active use: `code.png`/`code-selected.png`, `iran-date.png`/`iran-date-selected.png`/`iran-date-picker.png`, `iran-time.png`/`iran-time-selected.png`, `iran-checkout.png`, `iran-success.png`/`Error.png` (all reference-only) and `Calendar-form.svg`/`clock-form.svg`/`user-form.svg` (rendered in-page, `done.svg`/`xmark.svg` reused from Phase 4.2); Phase 5.1 additions now in active use (all reference-only): `abroad-time-zone.png`/`abroad-time-zone-selected.png`, `abroad-date.png`/`abroad-date-selected.png`/`abroad-date-picker.png`, `abroad-day.png`/`abroad-day-selected.png`, `abroad-checkout.png`, `abroad-success.png`; supplied but not yet wired into any component: `Hero.png`, `Navbar.png`, `full landing.png`, `section2.png`, `section3.png`, `section4.png`, `Footer.png`, `wrong-code.png`, `reserved-from-iran.png`, `reserved-from-abroud.png`.
**Removed:** `clinic-crm/public/hero.jpg` (superseded by `Hero-background.jpg`/`Hero-background.mp4`); `clinic-crm/components/BookingWizard.tsx` and `clinic-crm/app/landing.css` (Task 4, 2026-07-28); 40 unused reference-screenshot assets from `public/landing-assets/` (Task 4 — see that entry above for the full list).
**Not touched:** every dashboard component, `lib/auth.tsx`, `lib/types.ts`, `lambda/*`, `template.yaml`, all deploy scripts.
**Verified isolation:** the 5 `components/ui/*` files import only `react` and `lib/utils`'s `cn` helper (plus `Modal`→`Card` internally) — nothing dashboard/booking-related. `useBookingFlow.ts`, `bookingService.ts`, `locationService.ts`, and the booking step components import only `react` and `components/ui/*`/`lib/jalali.ts`/`lib/utils.ts`/`lib/locationService.ts` — no dashboard coupling.

## Remaining Phases & Next Step
Per the plan (§2/§9), with Phase 2b canceled: Phase 3.0–3.6 (done, approved — Phase 3 complete in full) → Phase 4.0 Controller skeleton (done, approved 2026-07-26) → Phase 4.1 New Patient intake step (done, approved 2026-07-26) → Phase 4.2 New Patient review + terminal states (done, approved 2026-07-26 — the entire New Patient persona complete end to end) → Phase 5.0 Existing Patient / Iran flow (done, approved 2026-07-27) → Phase 5.1 Existing Patient / Abroad extension (done, approved 2026-07-27 — the entire Existing Patient persona complete end to end) → **Phase 6 Integration & Polish (done, approved 2026-07-28) — the entire landing + booking migration (Phases 1–6) is now complete and regression-tested end to end.**

**Exact next step:** none — all of §2/§9's numbered phases and the full post-migration 6-task list (Tasks 1–6) are done, approved, and committed/pushed as of 2026-07-28. The only items still open are non-blocking, standing items, waivable at your discretion: §9's point 9, a **separate, later, explicit** discussion of whether/when to retire the old `BookingWizard.tsx` (untouched throughout this migration, per §3); and a written §7-format Visual QA report, never produced for any sub-phase since 3.1 (see the Unresolved/Flagged Items below).

**Asset substitutions actually in use (superseding the earlier planned filenames below, which were never used):**
- Logo: `public/logo.png` (local file, not the Figma logo asset). Present, in use.
- Hero background: `public/Hero-background.mp4` (video) with `public/Hero-background.jpg` as `poster` (local files, replacing Figma's static hero background image only — everything else in Hero stays Figma-faithful). Present, in use.
- Icons: `public/Phone, Call.svg`, `public/message-question-checkmark.svg`, `public/Arrow.svg`. Present, in use.
- Note: the originally-planned substitution filenames (`public/logo-2048-hq-transparent.png`, `public/3.mp4`) were superseded before implementation and were never used — not present in the repo.

## Unresolved / Flagged Items
- **Figma MCP rate-limit blocker — worked around, not resolved, and approved on that basis.** The Starter-plan rate limit that blocked Phase 2b and initially blocked Phase 3.1+3.2 was never lifted; instead, Navbar/Hero were built and refined from screenshots, manual measurements, and final assets you supplied directly, bypassing the need for further Figma MCP calls — and you approved the resulting implementation on that basis. This means Navbar/Hero are **not pixel-verified against Figma tool output** the way Button/Input/Card were in Phase 2 — flagged as a difference in provenance, not a blocker. Likely to recur for later Phase 3 sub-phases too (e.g. exact service/contact card treatment for 3.4/3.5) unless the same manual-asset workaround is used again.
- **Hero heading/subtext text-shadow value mismatch.** The heading uses `0 6px 17px rgba(0,0,0,0.49)`; the subtext currently uses a different value, `0 6px 7px rgba(0,0,0,0.65)` (smaller blur, higher opacity), rather than the single shared value originally specified. Recorded as the actual current state (changed on disk outside this conversation); flag for your review in case it wasn't intentional.
- **Phase 3.1–3.6 Visual QA reports (§7 format) not yet produced**, despite all six sub-phases (and Phase 3 as a whole) being approved. A written report with the required sections (what was implemented, what reference screenshots were compared, what was checked, mismatches, remaining uncertainty) has not been recorded for any of them. Flagged as an open item, not silently skipped — can be produced retroactively if useful, or waived explicitly, at your discretion.
- **Phase 3.6 nav-link "409px from the logo" figure not independently enforced.** Implemented per the spec's other stated intent ("centered within the footer"); the 409px distance was treated as a derived/consequential measurement rather than a separate hard constraint. Not yet explicitly confirmed by you.
- **`.next` build-cache corruption incident (Phase 3.6, resolved; recurred in Phase 4.1, Phase 4.2, Phase 5.0, and again in Phase 5.1, all resolved).** Not a recurring risk to the codebase, but worth remembering for future sessions: running `npm run build` while `npm run dev` is simultaneously active on the same project corrupts the dev server's webpack cache and can look exactly like "the page lost all styling" or a `Cannot find module './NNN.js'` crash (Phase 5.0's instance showed as compiled CSS/JS 404s). Fix is always: stop the dev server, delete `.next`, restart. Adopted going forward: the dev server is stopped before every `npm run build` verification run, not left running concurrently — this rule has been broken and re-triggered the incident more than once already, so it needs active attention each time a build check is run against a live dev server.
- **Important discovery, Phase 5.1: `app/globals.css`'s unlayered `button { border: none; background: none; color: inherit; }` reset (around line 125) silently defeats plain Tailwind border/background classes on ANY `<button>` in the app, regardless of specificity — same mechanism as the Phase 3.1 nav-link `a { color: ... }` bug, just discovered on a different element type.** It only stayed hidden this long because most existing buttons either already use `!important` (`Button.tsx`'s secondary variant: `!border !border-solid !border-neutral-100`) or don't need a visible default border/background. Any *new* custom-styled `<button>` added in a future phase needs the same `!` (important) modifier on its border/background utility classes, or the styling will silently not render. Worth a proactive sweep in Phase 6 to check nothing else is quietly affected.
- **Phase 5.1 Abroad location dataset (`lib/mock/locations.ts`) simplifications:** a single static `utcOffsetMinutes` per city — daylight-saving-time shifts are not modeled; the Iran↔local time conversion (`convertIranHourToLocal`) only handles same-day time-of-day conversion — a day-boundary crossover (e.g. a very negative-offset city where local time falls on the previous/next calendar day relative to Iran's date) isn't handled, since the design only shows a time value, not a shifted date. Both flagged as known limitations, not bugs. (Note: the static-per-city part was superseded by Task 2's swap to the real `lib/data/locations.ts` dataset with live `Intl`-computed offsets, which does account for current DST — the day-boundary-crossover limitation remains.)
- **Resolved in Task 6 (2026-07-28): a real crash, not just a known limitation.** Searching the Abroad location picker for a Kosovo or Somaliland city crashed with an uncaught `RangeError`, because 6 entries in the real dataset (introduced by Task 2) carry a non-standard `iso2: -99` placeholder instead of a real ISO code, and `Intl.DisplayNames.of(-99)` throws instead of returning `undefined`. Fixed in `lib/locationService.ts` by validating the ISO-code format before calling `Intl.DisplayNames.of()`. See Task 6 entry above for full detail.
- **Phase 5.1 foreign-time single-line "shrink to fit" is a static reduced font (`text-tiny`, 10px) plus an ellipsis fallback, not true per-string dynamic shrinking.** The longest country name in the mock dataset ("امارات متحده عربی") still doesn't fit combined with a time value even at 10px and visibly truncates with "…" — true dynamic shrink-to-fit would need JS text measurement, not otherwise used anywhere in this codebase. Flagged as a disclosed simplification, confirmed working as designed (truncation, not overflow or wrapping).
- **Phase 5.1 dropdown option format ("City (UTC+H:MM)") follows the reference screenshot literally**, not the prose spec's "GMT+1" shorthand — treated the screenshot as authoritative when the two conflicted. The screenshot's "or 5:36 am" live-current-time hint next to the selected value was intentionally left out (not explicitly requested, and would need a ticking clock).
- **Phase 5.1 checkout's time label still shows the location's city name in English** (e.g. "Berlin"), while the Time Selection step's cards show the Farsi country name (e.g. "آلمان") — only the Time step's country-name-in-Farsi correction was requested; checkout was never asked to change and was left as-is. Flag if this inconsistency should be reconciled later.
- **Phase 5.1 year-picker range (today's year `+5`, from Phase 5.0) is unchanged and still not explicitly confirmed** — same judgment call, now also governing the Abroad date step since it's the same shared component.
- **Phase 5.0 year-picker range is a judgment call, not yet confirmed.** The date step's year dropdown lists today's year through `+5` years — an arbitrary but reasonable finite window, since there's no Figma spec for this control and forward booking navigation itself has no stated upper bound. Not yet explicitly confirmed by you.
- **Phase 5.0 month/year dropdown styling has no Figma source.** Built as a plain white/bordered/shadowed list matching the app's general visual language (rounded-`8px`, `neutral-100` border, `primary-50`/`primary-500` selected state) since no reference screenshot shows this control (it was previously decorative-only). Not Figma-verified.
- **Phase 5.0 `444444` dev-only forced-payment-failure code.** Exists purely so the checkout Error terminal state is reachable without a real payment gateway — same spirit as Phase 4.2's name-substring triggers. Flagged (per `bookingService.ts`'s own comment) to remove once a real payment gateway replaces the mock.
- **Phase 5.0 checkout icon-badge shape (`ExistingPatientCheckoutStep.tsx`) is a judgment call.** Rendered as a rounded-full chip; the reference screenshot doesn't pixel-confirm circle vs. rounded-square. Not yet explicitly confirmed by you.
- **Phase 4.1 panel sizing/positioning values were adjusted directly in the file outside the conversation, after my last recorded pass.** Current state: entry panel `524×212px` (matches `Default Reserve.png`); New Patient intake panel `524×712px` at `top-[34px]`, inside a Hero section widened to `780px`. Recorded as the actual, approved current state — flag if any of these weren't intentional. (Phase 4.2's terminal-state panel, `524×220px` at `top-[280px]`, was derived from these same values and is confirmed correct.)
- **Phase 4.2 mock outcome logic is demo-only, not a real rule.** `submitBooking`'s Success/Error/No-service selection is driven by a static `onlineBookingEnabled` flag and two reserved name substrings (`ارور`, `تعطیل`) — purely so every terminal state is reachable without a backend. This has no equivalent in the real Figma design and would need real business logic (or at least a real settings source) before this migration is considered production-ready.
- **Phase 4.2 terminal heading/subtitle font sizes were a visual judgment call**, not pixel-sampled — `text-h6`/`text-caption` tokens were chosen to fit the fixed `220px` panel height without overflow, based on proportional comparison to the reference screenshots rather than exact measurement. Not yet explicitly confirmed by you.
- **Phase 3.5 `backdrop-blur` amount was a literal-value judgment call.** The spec stated "Background blur: 47%" — not a valid CSS length for `backdrop-filter: blur()`, which requires a length unit. Implemented as `47px`, the most plausible reading (Figma's own "Background blur" effect panel is normally a px value). Not yet explicitly confirmed by you.
- **Phase 3.5 card height and a few internal offsets (title/description top, overlay opacity) currently differ from the written spec** (card `152px` vs. spec's `144px`; overlay currently 50% vs. spec's 20%; title/description positions shifted a few px accordingly) — changed on disk outside the corrective conversation turns. Recorded as the actual current state, not reverted; flag for your review in case any of these weren't intentional.
- **Phase 3.3 feature-grid spacing/position values were inferred, not given as exact pixels.** The spec gave exact pixel positions for the heading/subtitle/intro text but only "use the layout shown in the screenshot" for the feature grid; the grid's column/row gaps and its vertical offset from the section top were estimated from the screenshot's proportions. Not yet explicitly confirmed by you.
- **Phase 3.4 flower asset filename mismatch:** the spec named `big-flower.png`/`little-flower.png`; only `.jpg` versions exist on disk and were used instead. Flagged for your awareness.
- **Phase 3.4 card shadow blur radius and section background color were inferred from the screenshot**, not given as exact spec values (shadow blur estimated at 20px; background read as white from the screenshot). Not yet explicitly confirmed by you.
- **Full browser-based visual QA against the supplied screenshots not yet logged**, for the same reason noted in Phase 0 (no browser-automation tool available in this environment) — verification performed and recorded so far is build/typecheck/compiled-CSS-level, not a documented side-by-side or overlay visual comparison.
- **Discovery for Phase 5:** the real "Reserved From Iran" stepper has 5 steps including a **"پرداخت" (Payment)** step not previously identified in the architecture report — factor this in when Phase 5 is planned.
- `Input`'s darker-border state is focus-triggered, not value-presence-triggered as Figma's "Filled" naming might imply — flagged judgment call, not yet confirmed by user.
- `TimezoneCombobox` recommended as a separate future primitive rather than folded into `Input` — not yet confirmed by user.
- `Modal`'s backdrop/dismiss styling has no Figma source; inherited from the current app's `.bw-overlay` pattern — flagged, not Figma-verified.
- `StepperIndicator`'s connector-line color reproduces Figma's literal `#c6c6c6`, which doesn't exactly match the nearest `neutral-200` token — small source-file inconsistency, not corrected.
- Two pre-existing, unrelated build warnings remain (not fixed, not in scope): `app/page.tsx` `useEffect` missing-dependency; `layout.tsx` metadata `viewport` deprecation.
- **Resolved in Phase 6 (2026-07-28):** full interactive dashboard walkthrough (login → all pages → logout) — previously only server-render-checked — was actually verified in a real browser and passed with no regressions.
- **Browser-automation tooling note (Phase 6, session 2):** immediately after a full page reload, simulated clicks from the browser-automation tool intermittently failed to register on the persona-toggle button (React state not updating) until dispatched via a direct DOM `.click()`; not observed at any other point, and not a product defect.

## Post-Migration Additional Tasks (New Requirements)
New requirements raised after Phase 6 was approved; tracked separately from the original phase numbering, same §0 rules apply.

- **Task 1 — Connect Landing Page to Dashboard, completed (2026-07-28), no files changed.** Inspected the old app (this repo's `main` branch) and found its only Landing→Dashboard route was the footer's "ورود کارکنان" button + `Alt+L`, both calling `onLogin`; no other separate pages were linked from the landing page. `Alt+L` was already wired since Phase 3.0 and never broke — the footer's visible button was the one piece never actually added in Phase 3.6. Asked whether to add it now; **you chose to keep `Alt+L` only**, so no code changed. Re-verified end-to-end: Landing → `Alt+L` → Login → Dashboard → Patients → Calendar → Logout → Landing, all correct; booking-flow entry point unaffected; `tsc`/`build` clean. One pre-existing, unrelated console error seen on Dashboard load (`DynamoDBService` fetch failure — no backend deployed locally, out of scope). See Migration Plan §10 for full detail.
- **Task 2 — Replace mock location data with a real solution, completed (2026-07-28), including a post-review adjustment.** `lib/mock/locations.ts` (15-city mock) replaced with a real ~3,900-city dataset (`lib/data/locations.ts` + generated JSON), sourced from the free `city-timezones` npm package via `scripts/generate-locations-data.mjs` (devDependency only, not shipped to the browser), loaded via dynamic `import()` so the Landing Page's initial bundle size is unaffected (121 kB, unchanged). `lib/locationService.ts` keeps its exact prior interface but now computes Farsi country names and UTC offsets live via native `Intl` APIs (also fixes the earlier static/no-DST limitation). **Adjustment, your explicit request:** Iranian cities are excluded from the dataset (this picker only serves the Abroad flow) — applied in the generator script, dataset now 3,897 cities; UI, search behavior, and performance unchanged. Verified with real cities never in the old mock (Osaka, Manchester, Dubai) end-to-end to Success and to the forced-failure screen; confirmed "Tehran" now returns no results while Dubai still works; Iran flow and Task 1's `Alt+L` link re-confirmed unaffected; `tsc`/`build` clean, no console errors. See Migration Plan §10 for full detail.
- **Additional UX improvement — Persian/English digit handling, completed (2026-07-28).** Requested between Task 2 and Task 3. The shared `Input` primitive (`components/ui/Input.tsx`) gained an opt-in `numeric` prop, applied only to the reference-code field and the phone-number field: always displays Persian digits, accepts English or Persian digits typed or pasted, strips any non-digit character, and hands the value back through the *same* `onChange` shape already-normalized to English digits — so `ExistingPatientCodeStep`'s `/^\d{6}$/` check and `bookingService`'s reference-code lookup needed no changes at all. Verified: typing `123abc456` displays `۱۲۳۴۵۶`; typing Persian digits `۱۱۱۱۱۱` and submitting correctly resolves the real Iran patient (proving true internal normalization, not just display); a simulated paste of mixed digits + a hyphen normalizes correctly; full New Patient submission with the new phone field completed successfully. `tsc`/`build` clean, no console errors. See Migration Plan §10 for full detail.
- **Task 3 — Asset organization, completed and approved (2026-07-28).** You had already created `public/landing-assets/` and moved every Landing Page/booking asset into it; this task was purely updating code references. Updated 34 path strings across 9 files (`LandingPage.tsx` 21, `ExistingPatientCheckoutStep.tsx` 3, `NewPatientTerminalStep.tsx` 3, `ExistingPatientTerminalStep.tsx` 2, one `Arrow.svg` reference each in the five other step components) to point at `/landing-assets/...`. No files were moved/renamed/replaced — path strings only. Deliberately left `app/landing.css`'s two `/hero.jpg` references untouched (dead, unimported file; `hero.jpg` no longer exists on disk either). Verified via actual network-request inspection (not just visual spot-checking) across every code path that touches an asset — all 26 distinct referenced files return 200/206/304, zero 404s; `tsc`/`build` clean, bundle size unchanged, no console errors. See Migration Plan §10 for full detail.
- **Task 4 — Project cleanup, completed and approved (2026-07-28).** Removed 40 unused design-reference screenshots/icons from `public/landing-assets/` (only the 26 files Task 3 confirmed are actually rendered remain), confirmed unused via exhaustive project-wide grep — every match was a source comment documenting design provenance, never a functional reference. Also removed `components/BookingWizard.tsx` and `app/landing.css`, per your explicit decision overriding §3's standing "separate discussion" rule (surfaced to you first, not assumed) — both confirmed to have zero functional references anywhere. Nothing else touched (docs, deploy/infra config, in-use assets all left alone). Full functional regression re-verified (Landing Page, all three booking personas, Task 1's `Alt+L` link); `tsc`/`build` clean, bundle size unchanged; zero new 404s (one pre-existing, unrelated `favicon.ico` 404 noted, not caused by this task); no console errors. See Migration Plan §10 for full detail.
- **Task 5 — Limited clean-code review of the Landing Page implementation, completed and approved (2026-07-28).** Reviewed `LandingPage.tsx` and everything it directly wires in (booking-flow components, `useBookingFlow`, `bookingService`, `locationService`, `jalali.ts`, the `components/ui/*` primitives it uses) for duplicate code, dead code, unused imports/components, type safety, and readability. Four low-risk changes made: removed the unused `onBookingSubmit` prop (and its `any` type) from `LandingPage.tsx` — never called in the body, never passed by its only caller; added a shared `parseDateKey()` helper to `lib/jalali.ts` (the inverse of the existing `dateKeyFor()`) and used it to replace four duplicated inline `key.split("-").map(Number)` occurrences across `LandingPage.tsx` (3) and `ExistingPatientTimeStep.tsx` (1); removed the unused `export default` from `lib/mock/bookingService.ts` and `lib/locationService.ts` (every consumer already used the named export). **Flagged, not changed:** `components/ui/Card.tsx`/`Modal.tsx` are fully unused anywhere in the app but were deliberately kept available as Phase 2 design-system primitives per an earlier decision — left in place pending a separate explicit call; `lib/utils.ts`'s unused `getPascalCase` is pre-existing dashboard-shared utility, out of this task's Landing-Page scope; `bookingService`'s unused `verifyOtp`/`lookupPatientByPhone` stubs are already documented as an intentional placeholder, not accidental dead code. No UI, booking logic, or business rules changed. Verified: `tsc`/`build` clean (same two pre-existing warnings, bundle size unchanged); live browser regression of Existing Patient/Iran (full flow to payment success, confirming `parseDateKey` produces identical labels), New Patient (intake → success), and `Alt+L` → Login — no console errors. See Migration Plan §10 for full detail.
- **Task 6 — Final QA before commit and push, completed and approved (2026-07-28). This is the last item on your 6-task post-migration list — the project has been committed and pushed.** Re-verified `tsc`/`build` clean, then re-ran the full regression suite: Landing Page rendering/assets/navigation (zero broken assets across 30 page-load requests), New Patient (success + `ارور` error), Existing Patient/Iran (wrong-code error + `111111` full success), Existing Patient/Abroad (real city search + Location/dual-calendar/dual-time/Checkout/Success, plus `555555` forced-payment-failure + retry), and `Alt+L` → Login → Dashboard → Patients → Calendar → Requests → Notifications → Settings → Logout. **Bug found and fixed:** searching the real Abroad location dataset for a Kosovo or Somaliland city crashed the picker with an uncaught `RangeError` — 6 dataset entries (disputed territories) carry a non-standard `iso2: -99` placeholder from the upstream `city-timezones` package instead of a real ISO code, and `Intl.DisplayNames.of(-99)` throws instead of returning `undefined`, so `lib/locationService.ts`'s existing `?? raw.country` fallback never ran. Fixed in `lib/locationService.ts` only, by validating the ISO-code format (plus a try/catch) before calling `Intl.DisplayNames.of()` — same defensive pattern already used by the adjacent `getUtcOffsetMinutes` helper. This bug pre-dated this session (introduced by Task 2's real-dataset swap) and was only now surfaced by searching one of these specific disputed-territory names for the first time. Verified: "Kosovo" search now returns Pristina/Pec with no crash (graceful English-name fallback); unaffected entries (e.g. Berlin → "آلمان") still translate correctly. No UI, design, or booking-logic/business-rule changes. See Migration Plan §10 for full detail.
